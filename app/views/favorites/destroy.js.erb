var favoritePage = document.getElementById("favorite");
var establishmentCard = document.getElementById("establishment-<%= @establishment.id %>");
if (favoritePage) {
  establishmentCard.remove();
} else {
  establishmentCard.innerHTML = "<%= j render 'establishments/card_index', establishment: @establishment %>";
}
